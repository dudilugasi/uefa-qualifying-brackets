{
  "name": "ucl-qualifying-bracket",
  "private": true,
  "version": "1.0.0",
  "description": "Champions League qualifying bracket, scraped on demand",
  "scripts": {
    "start": "node scripts/server.mjs",
    "dev": "node --watch scripts/server.mjs",
    "refresh": "node scripts/refresh.mjs",
    "refresh:remote": "gh workflow run 'Refresh data' --repo dudilugasi/uefa-qualifying-brackets"
  }
}
